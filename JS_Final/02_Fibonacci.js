/**function fbncci
 * @description mostrar un array con los numeros de fibonacci
 * @param {number} limite
 * @returns {array}
 */




function fbncci(n) {
      if (limite < 1) {
            return []
      }
      const final = [0, 1]
      do {
            
      } while (condition);
      for (let i = 2; i < n; i++) {
        final.push(a + b)
      const a = (i - 1)
      const b = (i - 2)
      }
      return final[n]
    
    console.log(fbncci(8))